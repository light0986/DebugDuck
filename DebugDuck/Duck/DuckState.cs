namespace DebugDuck.Duck
{
    /// <summary>小鴨的動畫狀態。每個值對應 Assets 下的一個同名資料夾。</summary>
    public enum DuckState
    {
        Idle,
        Blink,
        Listen,
        Talk,
        Happy,
        Drag
    }
}
